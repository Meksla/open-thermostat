
import Queue

toZoneManager = Queue.Queue(0)
toNetworkManager = Queue.Queue(0)
toUnitManager = Queue.Queue(0)
toScheduleManager = Queue.Queue(0)
toLearningAlgorithm = Queue.Queue(0)
toControllerAlgorithm = Queue.Queue(0)

enabledZones = []
enabledUnits = []



